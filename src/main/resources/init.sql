CREATE TABLE beat
(
    id    BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name  VARCHAR(255),
    genre VARCHAR(255),
    image VARCHAR(255),
    price DECIMAL,
    CONSTRAINT pk_beat PRIMARY KEY (id)
);